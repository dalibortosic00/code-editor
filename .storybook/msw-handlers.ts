// MSW handlers disabled in this environment to avoid ESM interop issues when importing `msw` in Storybook test setup.
// Add targeted handlers here (using `rest`) if your local Storybook/Vite environment supports the installed `msw` version.
export const mswHandlers: unknown[] = []; 
